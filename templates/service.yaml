apiVersion: v1
kind: Service
metadata:
  name: {{ include "dns-admin.fullname" . }}
  labels:
    {{- include "dns-admin.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dns-admin.selectorLabels" . | nindent 4 }}
